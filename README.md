# Сравнение производительности ArrayList с LinkedList

Сравнение методов `add()`, `get()`, `remove()` по индексу с начала и с конца, инициализации листов.

![Табличка с чиселками](images/perf_test_image.png)
